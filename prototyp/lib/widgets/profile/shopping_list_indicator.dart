import 'package:flutter/material.dart';

class ShoppingListIndicator extends StatelessWidget {
  const ShoppingListIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.list_alt);
  }
}
